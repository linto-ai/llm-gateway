# LLM Gateway (working one i hope)

no docker file yet
can be started using after installing requirements.

```
python -m app
```

tests would use 

```
python3 -m tests
```
but none yet

next head to host:port/docs for swagger