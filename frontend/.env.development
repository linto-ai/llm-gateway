# Local development defaults
# These are embedded at build time by Next.js during `npm run dev`
# Docker builds ignore this file and use placeholders instead
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
